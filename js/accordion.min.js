document.addEventListener("DOMContentLoaded", function() {
    let t = document.getElementsByClassName("accordion"),
        e = document.getElementsByClassName("panel"),
        i = !0;
    for (let t = 0; t < e.length; t++) {
        let i = e[t].offsetHeight;
        e[t].setAttribute("data-height", i + "px"), e[t].style.height = 0
    }
    for (let l = 0; l < t.length; l++) t[l].onclick = function() {
        let a = this.getAttribute("data-open");
        if (this.classList.contains("active")) {
            this.classList.remove("active");
            for (let t = 0; t < e.length; t++) e[t].getAttribute("data-panel") === a && (e[t].style.height = 0);
            return !1
        }
        if (i) {
            for (let e = 0; e < t.length; e++) t[e].classList.remove("active");
            for (let t = 0; t < e.length; t++) e[t].style.height = 0
        }
        this.classList.add("active");
        for (let t = 0; t < e.length; t++) e[t].getAttribute("data-panel") === a && (e[t].style.height = e[l].getAttribute("data-height"));
        return !1
    }
});